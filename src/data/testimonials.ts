import type { Testimonial } from '../types';

// Publish only reviews with a verifiable source and permission to display.
// The former entries had no source provenance in the repository.
export const testimonials: Testimonial[] = [];
