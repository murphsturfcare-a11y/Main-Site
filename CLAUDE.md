@AGENTS.md

<!-- Multi-agent tmux orchestration (QB dispatch mode) is opt-in, not the default.
     Only follow .claude/qb-dispatch.md when explicitly asked to run QB dispatch mode
     AND the "agentic:Murphy's Turf" tmux session with specialist panes is running. -->
